// import { loadWorkouts } from './load-workouts';

export const saveWorkout = async (workout) => {
  // const allWorkouts = await loadWorkouts();
  // const updatedWorkouts = [...allWorkouts, workout];
  // localStorage.setItem('po_workouts', JSON.stringify(updatedWorkouts));
};
